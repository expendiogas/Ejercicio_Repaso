CONCEPTOS Y EJERCICIOS POR Fernando
1. Para que sirve y como se utiliza: git config --global user.name 
	R// Sirve para establecer o cambiar mi nombre de usuario en el git bash , para hacerlo pongo el comando y luego mi nombre de usuario deseado y enter
2. Para que sirve y como se utiliza: git config --global user.email
	R// Sirve para establecer o cambiar mi correo en el git bash. Al igual que con el usuario, si opera el comando con el correo escrito
3. Como ver la version de GIT?
	R// Se debe ejecutar el comando git --version en el git bash para saber cual es

CONCEPTOS Y EJERCICIOS POR MACO
1. Para que sirve y como se utiliza: GIT CLONE
2. Para que sirve y como se utiliza: GIT STATUS
3. Para que sirve y como se utiliza: GIT ADD
4. Para que sirve y como se utiliza: GIT COMMIT
5. Para que sirve y como se utiliza: GIT PUSH

CONCEPTOS Y EJERCICIOS POR BYRON
1. DIFERENCIA ENTRE GIT Y GITHUB:
2. QUE ES UNA RAMA EN GIT?
3. COMO CREAR UNA RAMA?
4. COMO FUSIONAR UNA RAMA?
5. SUBIR UN ARCHIVO EDITABLE A ESE REPOSITORIO, CUALQUIER ARCHIVO (TXT, WORD, EXCEL, PPT)

CONCEPTOS Y EJERCICIOS POR EDGAR
1. CARGAR UN ARCHIVO TXT CON UN SP SIMPLE DE UNA DB
2. IDEAR 3 O MAS CAMBIOS AL SP Y ASIGNAR AL PERSONAL REALIZAR ESOS CAMBIOS (INCLUIDO HACER UN CAMBIO USTED MISMO)

CONCEPTOS Y EJERCICIOS POR ISMAR 
1. Como ver la version de GIT? 
	R// Se debe utilizar el comando git --version dentro del git bash
2. Para que sirve y como se utiliza: GIT PUSH
	R// Sirve para subir un cambio a GitHub luego de realizar un commit 
3. SUBIR UN ARCHIVO EDITABLE A ESE REPOSITORIO, CUALQUIER ARCHIVO (TXT, WORD, EXCEL, PPT)
4.CARGAR UN ARCHIVO TXT CON UN SP SIMPLE DE UNA DB
	R// Se cargó un archivo con nombre SP_GrabaImagen:
		Edgar: Hacer que el sp también grabe el archivo en una tabla de nombre MMarca en un campo ImagenMarca. Confirmar cuando este:__________________
		Maco: Hacer que el campo flgImg dentro de MaeS cambie a True cuando se ejecute el SP. Confirmar cuando este:__________________
		Byron: Hacer que el sp cambie a True el campo flgIMG dentro de MMArca cuando se ejecute. Confirmar cuando este:__________________
